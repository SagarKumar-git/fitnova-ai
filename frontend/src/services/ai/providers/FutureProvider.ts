import { MockProvider } from './MockProvider';
// Simple stub representing a potential future integration (e.g. ClaudeProvider)
export class FutureProvider extends MockProvider {
  name = 'FutureProvider';
}
